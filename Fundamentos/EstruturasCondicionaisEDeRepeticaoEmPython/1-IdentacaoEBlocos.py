""" Utilizando Espaços """
""" Existe uma convenção em python que define as boas práticas para escrita de um código na linguagem. Nesse documento é indicado utilizar 4 espaços em branco por nivel de identação, ou seja, a cada novo bloco adcionamos 4 novos espaços em branco"""

""" Bloco em Python """
def sacar (self, valor: float) => None: # ini do bloco do bloco do método

    if self.saldo >= valor: # início do blodo do if
        self.saldo -= valor
    # fim do bloco do if

# fim do bloco do método
